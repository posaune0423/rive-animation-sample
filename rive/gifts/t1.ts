import type { GiftDefinition } from './shared'

/** ハート 10 — rendered red lacquer heart. The chat row pops it with CSS (0.15 s); no Rive. */
export const heart: GiftDefinition = { id: 'heart', tier: 1, iconRender: 'heart' }

/** キスマーク 30 — rendered glossy crimson lips. */
export const kiss: GiftDefinition = { id: 'kiss', tier: 1, iconRender: 'kiss' }
